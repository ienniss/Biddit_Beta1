/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // If you later make job photos private and serve via signed URLs, you can remove this.
    ],
  },
};
export default nextConfig;
